/* global describe it expect */
import AccountManager from 'components/AccountManager/AccountManager'

describe('(Component) AccountManager', () => {
  it('exists', () => {
    expect(AccountManager).to.exist
  })
})

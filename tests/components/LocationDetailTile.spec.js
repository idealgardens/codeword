/* global describe it expect */
import LocationDetailTile from 'components/LocationDetailTile/LocationDetailTile'

describe('(Component) LocationDetailTile', () => {
  it('exists', () => {
    expect(LocationDetailTile).to.exist
  })
})

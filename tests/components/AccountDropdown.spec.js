/* global describe it expect */
import AccountDropdown from 'components/AccountDropdown/AccountDropdown'

describe('(Component) AccountDropdown', () => {
  it('exists', () => {
    expect(AccountDropdown).to.exist
  })
})
